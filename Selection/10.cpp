#include <iostream>

using namespace std;
int main()
{
    int num,max;
    cin >> num; 
    max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num);
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cin >> num;
    if(max < num)
        max = num;
    cout << max << endl;
}
